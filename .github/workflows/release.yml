name: Release

# Push a tag matching `v*` (e.g. `v0.2.0`) to build every platform below
# and publish a GitHub Release with short notes plus each platform's
# archive attached. `workflow_dispatch` runs the exact same build matrix
# without publishing anything — a safe way to test this pipeline (e.g.
# after editing this file) without cutting a real tag/release each time;
# its results just show up as this workflow run's own downloadable
# artifacts instead.
#
# arm64 Windows/Linux builds are deliberately not included yet (noted,
# not forgotten) — GitHub-hosted runners for those are less mature than
# the four targets here; add them as their own matrix entries once
# that's worth revisiting.
#
# Release notes come from CHANGELOG.md's top `## ...` section verbatim
# (see the `release` job below) — following "Keep a Changelog" style,
# that section is renamed from `## Unreleased` to `## vX.Y.Z` (with a
# fresh empty `## Unreleased` added above it) and `[workspace.package]
# version` in `Cargo.toml` is bumped to match, before the tag is pushed.
# Not enforced by this workflow itself — see `.waypoint/skills/
# version-bump.md`, which handles the whole thing on request (bump,
# commit, push to main, and — with the developer's explicit
# confirmation each time — tag and push the tag too).
#
# `pages` publishes everything served from the `gh-pages` branch — the
# GPG-signed APT and DNF repositories and the AppImage (see that job for
# the full mechanics). Like `release`, it only runs on a real tag push: a
# plain `workflow_dispatch` build test shouldn't have the side effect of
# publishing to the real public repositories. `gh-pages` itself was
# bootstrapped once, directly, rather than by this workflow (see that
# job's own comment) — no manual-dispatch escape hatch is needed for it.
#
# Packages are *not* verified here. `verify-packages.yml` installs each
# one into a stock image of the distribution it targets and starts the
# application for real; it runs on main and on demand, so a tag is pushed
# only once that has already passed. Gating the release path on it would
# make every release wait for work that's already been done.

"on":
  push:
    tags:
      - "v*"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        # `artifact` is the upload name. The two macOS entries are
        # deliberately *not* named `pain-*`: they're intermediate raw
        # binaries consumed by `macos-bundle`, and the `release` job
        # selects only `pain-*` so these never get attached to a release
        # as downloads of their own.
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            name: windows-x86_64
            artifact: pain-windows-x86_64
          - os: macos-latest
            target: x86_64-apple-darwin
            name: macos-x86_64
            artifact: macos-raw-x86_64
          - os: macos-latest
            target: aarch64-apple-darwin
            name: macos-arm64
            artifact: macos-raw-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build (Windows/macOS)
        run: cargo build --release --target ${{ matrix.target }} -p pain

      # macOS gets no archive here — just the bare binary, which the
      # `macos-bundle` job below `lipo`s together with the other
      # architecture's into a single universal `pain.app`. Uploading a
      # per-architecture tarball too would mean shipping three macOS
      # downloads where one correct one will do.
      - name: Stage raw binary (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          cp "target/${{ matrix.target }}/release/pain" dist/

      - name: Package archive (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $stage = "pain-${{ matrix.name }}"
          New-Item -ItemType Directory -Path $stage | Out-Null
          Copy-Item "target/${{ matrix.target }}/release/pain.exe" $stage/
          Copy-Item LICENSE, README.md $stage/
          Compress-Archive -Path "$stage/*" -DestinationPath "$stage.zip"
          New-Item -ItemType Directory -Path dist | Out-Null
          Move-Item "$stage.zip" dist/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/*
          if-no-files-found: error

  # Linux is built in a container rather than on the runner, and so gets
  # its own job instead of a matrix entry.
  #
  # The reason is glibc: it's backward compatible but not forward
  # compatible, so a binary carries a floor from whatever it was linked
  # against. Building on `ubuntu-latest` means that floor rises silently
  # every time GitHub updates the runner image, cutting off users of older
  # distributions with no change on our side. `Containerfile` pins the
  # build to `ubuntu:22.04`, fixing the floor at glibc 2.35 until it's
  # moved deliberately.
  #
  # It also produces all four Linux artifacts from one compile — tarball,
  # .deb, .rpm, and AppImage — via the same script a developer runs
  # locally, so what CI builds and what can be reproduced by hand are the
  # same thing by construction.
  linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build the Linux packages
        run: ENGINE=docker ./scripts/linux-packages.sh build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: pain-linux-x86_64
          path: dist/*
          if-no-files-found: error

  # Combines the two macOS builds into one universal `pain.app`, so Mac
  # users get a single download that works on both Intel and Apple
  # Silicon instead of having to know which they have. Runs on a macOS
  # runner because `lipo` and `iconutil` are Apple tooling — neither has
  # a usable equivalent elsewhere, and `iconutil` is the only way to
  # produce a `.icns` Apple's own tools consider correct.
  macos-bundle:
    needs: build
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download the per-architecture macOS binaries
        uses: actions/download-artifact@v4
        with:
          pattern: macos-raw-*
          path: raw

      - name: Assemble the universal pain.app
        shell: bash
        run: |
          set -euo pipefail

          # Tag runs give `v1.2.3`; a `workflow_dispatch` test run gives a
          # branch name, which isn't a version at all — fall back rather
          # than writing something nonsensical into the bundle.
          version="${GITHUB_REF_NAME#v}"
          case "$version" in
            [0-9]*) ;;
            *) version="0.0.0" ;;
          esac
          echo "Bundling version $version"

          app="pain.app"
          mkdir -p "$app/Contents/MacOS" "$app/Contents/Resources"

          lipo -create -output "$app/Contents/MacOS/pain" \
            raw/macos-raw-x86_64/pain \
            raw/macos-raw-arm64/pain
          chmod +x "$app/Contents/MacOS/pain"

          sed "s/__VERSION__/${version}/g" assets/macos/Info.plist > "$app/Contents/Info.plist"

          # Apple's fixed `.iconset` filename convention — the @2x entries
          # are the same pixel dimensions as the next size up, which is
          # why several source PNGs are used twice.
          iconset="pain.iconset"
          mkdir -p "$iconset"
          cp assets/pain-16.png   "$iconset/icon_16x16.png"
          cp assets/pain-32.png   "$iconset/icon_16x16@2x.png"
          cp assets/pain-32.png   "$iconset/icon_32x32.png"
          cp assets/pain-64.png   "$iconset/icon_32x32@2x.png"
          cp assets/pain-128.png  "$iconset/icon_128x128.png"
          cp assets/pain-256.png  "$iconset/icon_128x128@2x.png"
          cp assets/pain-256.png  "$iconset/icon_256x256.png"
          cp assets/pain-512.png  "$iconset/icon_256x256@2x.png"
          cp assets/pain-512.png  "$iconset/icon_512x512.png"
          cp assets/pain-1024.png "$iconset/icon_512x512@2x.png"
          iconutil -c icns "$iconset" -o "$app/Contents/Resources/pain.icns"

          # Ad-hoc sign the bundle (`-s -`, no certificate involved, free).
          # The binary inside already carries the linker's own ad-hoc
          # signature, but that says nothing about the *bundle* — without
          # this step there's no `Contents/_CodeSignature/CodeResources`,
          # so macOS sees an unsealed bundle whose Info.plist and
          # resources aren't covered by anything. This does not replace
          # real signing/notarization (deliberately not purchased — see
          # `.waypoint/project.md`); users still clear quarantine by hand.
          codesign --force --sign - "$app"

          # Prove the bundle is actually what it claims to be before it
          # ships: both architectures present, the plist readable by
          # Apple's own parser with the version substituted, and the
          # bundle seal actually present and self-consistent.
          lipo -info "$app/Contents/MacOS/pain"
          lipo -archs "$app/Contents/MacOS/pain" | grep -q x86_64
          lipo -archs "$app/Contents/MacOS/pain" | grep -q arm64
          plutil -lint "$app/Contents/Info.plist"
          test "$(plutil -extract CFBundleShortVersionString raw -o - "$app/Contents/Info.plist")" = "$version"
          test -s "$app/Contents/Resources/pain.icns"
          test -f "$app/Contents/_CodeSignature/CodeResources"
          codesign --verify --strict --verbose=2 "$app"

          mkdir -p dist
          tar -czf dist/pain-macos-universal.tar.gz "$app" LICENSE README.md

      - name: Upload the macOS app
        uses: actions/upload-artifact@v4
        with:
          name: pain-macos-universal
          path: dist/*
          if-no-files-found: error

  release:
    needs: [build, linux, macos-bundle]
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      # `pattern: pain-*` deliberately excludes the `macos-raw-*`
      # intermediates — those are inputs to `macos-bundle`, not things a
      # user should be offered as a download.
      - name: Download the release artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: pain-*
          path: dist
          merge-multiple: true

      # Everything under the heading matching *this tag* specifically —
      # not just "the first `## ` heading found". `version-bump.md`'s own
      # process leaves a permanent empty `## Unreleased` heading sitting
      # above every past `## vX.Y.Z` section (that's the whole point — a
      # place for the next round of entries to land), so "first heading"
      # matched `## Unreleased` and grabbed nothing: confirmed on v1.0.0's
      # actual published release, whose notes came out empty. Matching
      # the tag name directly is what makes this correct release over
      # release, not just for a one-off fix.
      - name: Extract release notes from CHANGELOG.md
        env:
          TAG: ${{ github.ref_name }}
        run: |
          awk -v tag="## ${TAG}" '
            $0 == tag { found=1; next }
            /^## / && found { exit }
            found { print }
          ' CHANGELOG.md > release_notes.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          body_path: release_notes.md
          files: dist/*
          fail_on_unmatched_files: true

  # Publishes everything the `gh-pages` branch serves, in one job and one
  # push: the GPG-signed APT repository, the GPG-signed DNF repository,
  # and the AppImage.
  #
  # One job, not three, specifically because they all commit to the same
  # branch — separate jobs would race each other's pushes and one would
  # lose. Combining them also means a release updates every channel
  # atomically or none of them.
  #
  # Deliberately lightweight tooling for a single-package, single-
  # architecture repo: `dpkg-scanpackages`/`apt-ftparchive` and
  # `createrepo_c`, not reprepro/aptly. The APT half was verified
  # end-to-end against a real `apt` before it was written; the packages
  # themselves are verified by `Containerfile`'s test stages, which
  # install each one into a stock image of its distribution and start the
  # application for real.
  pages:
    needs: [linux]
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download Linux artifacts
        uses: actions/download-artifact@v4
        with:
          name: pain-linux-x86_64
          path: incoming

      # `rpm` supplies `rpmsign`; `createrepo-c` builds the DNF metadata.
      # Both are packaged on Debian/Ubuntu, which is what lets one
      # Debian-family environment publish an RPM repository at all.
      - name: Install repo-building tools
        run: |
          sudo apt-get update
          sudo apt-get install -y dpkg-dev apt-utils gnupg rpm createrepo-c

      # `--multiversion` (below) is what makes it safe to layer this
      # secret import into the same job that keeps accumulating
      # historical `.deb`s in `pool/` — each version's own metadata
      # survives even once a newer one exists.
      - name: Import the repo signing key
        env:
          APT_SIGNING_KEY: ${{ secrets.APT_SIGNING_KEY }}
        run: |
          echo "$APT_SIGNING_KEY" | gpg --batch --import
          KEYID="$(gpg --list-secret-keys --with-colons | awk -F: '/^sec/ {print $5; exit}')"
          echo "KEYID=$KEYID" >> "$GITHUB_ENV"
          # rpmsign finds its key by name through this macro rather than a
          # command-line flag, so it has to be on disk before signing.
          printf '%%_gpg_name %s\n' "$KEYID" > "$HOME/.rpmmacros"

      # `gh-pages` is bootstrapped once (an empty orphan branch with just
      # `.nojekyll`/`index.html`) and always exists from here on, so this
      # is a plain checkout — no "create it if this is the first run"
      # fallback needed. (An earlier version tried to do that fallback
      # inline with `continue-on-error`, but `actions/checkout` partially
      # initializes the target directory — `git init` plus the `origin`
      # remote — even when the ref-resolution step itself fails, so the
      # fallback's own `git remote add origin` collided with a remote
      # that was already there. Simpler to guarantee the branch exists
      # than to make that fallback correctly idempotent.)
      - name: Check out gh-pages
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: pages

      - name: Rebuild the APT repository
        working-directory: pages
        run: |
          set -euo pipefail
          touch .nojekyll
          mkdir -p pool
          cp ../incoming/*.deb pool/
          dpkg-scanpackages --multiversion pool > Packages
          gzip -9c Packages > Packages.gz
          rm -f Release
          apt-ftparchive release . > Release
          gpg --batch --yes --local-user "$KEYID" --detach-sign --armor -o Release.gpg Release
          gpg --batch --yes --local-user "$KEYID" --clearsign -o InRelease Release
          gpg --export --armor "$KEYID" > pain-archive-keyring.asc

      # RPM signs at two levels where APT signs at one: `gpgcheck=1`
      # verifies a signature embedded in each package, and
      # `repo_gpgcheck=1` verifies a detached signature over the repo
      # metadata. Both are done here so the `.repo` file below can turn
      # both checks on.
      - name: Rebuild the DNF repository
        working-directory: pages
        run: |
          set -euo pipefail
          mkdir -p rpm
          cp ../incoming/*.rpm rpm/
          rpm --addsign rpm/*.rpm
          createrepo_c --update rpm
          rm -f rpm/repodata/repomd.xml.asc
          gpg --batch --yes --local-user "$KEYID" --detach-sign --armor rpm/repodata/repomd.xml
          cat > rpm/pain.repo <<EOF
          [pain]
          name=pain
          baseurl=https://${{ github.repository_owner }}.github.io/pain/rpm
          enabled=1
          gpgcheck=1
          repo_gpgcheck=1
          gpgkey=https://${{ github.repository_owner }}.github.io/pain/pain-archive-keyring.asc
          EOF

      # A fixed path, overwritten each release rather than published under
      # a versioned name. The AppImage embeds this URL as its update
      # information, and zsync can only find the new build if the URL
      # doesn't move.
      - name: Publish the AppImage
        working-directory: pages
        run: |
          set -euo pipefail
          mkdir -p appimage
          cp ../incoming/pain-x86_64.AppImage appimage/
          cp ../incoming/pain-x86_64.AppImage.zsync appimage/

      - name: Rewrite the landing page
        working-directory: pages
        run: |
          cat > index.html <<EOF
          <!doctype html><meta charset="utf-8"><title>pain — Linux packages</title>
          <h1>pain</h1>
          <h2>Debian / Ubuntu</h2>
          <pre>
          curl -fsSL https://${{ github.repository_owner }}.github.io/pain/pain-archive-keyring.asc | sudo gpg --dearmor -o /etc/apt/keyrings/pain.gpg
          echo "deb [signed-by=/etc/apt/keyrings/pain.gpg] https://${{ github.repository_owner }}.github.io/pain ./" | sudo tee /etc/apt/sources.list.d/pain.list
          sudo apt update &amp;&amp; sudo apt install pain
          </pre>
          <h2>Fedora / RHEL</h2>
          <pre>
          sudo dnf config-manager --add-repo https://${{ github.repository_owner }}.github.io/pain/rpm/pain.repo
          sudo dnf install pain
          </pre>
          <h2>Any other distribution</h2>
          <pre>
          curl -fLO https://${{ github.repository_owner }}.github.io/pain/appimage/pain-x86_64.AppImage
          chmod +x pain-x86_64.AppImage
          ./pain-x86_64.AppImage
          </pre>
          EOF

      - name: Commit and push gh-pages
        working-directory: pages
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Publish ${{ github.ref_name }} packages" || echo "Nothing new to commit"
          git push origin HEAD:gh-pages
